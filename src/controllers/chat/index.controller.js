import { addMessage } from "#controllers/chat/addMessage.controller.js";
import { getMessages } from "#controllers/chat/getMessages.controller.js";

export const chatController = {
  addMessage,
  getMessages,
};
