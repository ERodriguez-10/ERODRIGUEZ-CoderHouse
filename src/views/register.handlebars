<div
  class="min-h-screen"
  style="background-size: cover; background-repeat: no-repeat; background-image: linear-gradient(to bottom, rgba(0,0,0,0.6) 0%,rgba(0,0,0,0.6) 100%), url('/assets/fondo-der.jpg');"
>
  <div class="container mx-auto">
    <div class="flex h-full items-center justify-end">
      <div
        class="text-black bg-slate-100 min-w-[550] rounded-2xl box-shadow: 0 2.8px 2.2px rgba(0, 0, 0, 0.034), 0 6.7px 5.3px rgba(0, 0, 0, 0.048), 0 12.5px 10px rgba(0, 0, 0, 0.06), 0 22.3px 17.9px rgba(0, 0, 0, 0.072), 0 41.8px 33.4px rgba(0, 0, 0, 0.086), 0 100px 80px rgba(0, 0, 0, 0.12) py-6"
      >
        <h1 class="text-center text-4xl font-bold pt-8">
          <i class="fa-solid fa-lock me-2"></i>
          Sing up
        </h1>
        <p class="text-center mt-3 mb-6 text-gray-600">Create an account</p>

        <form
          id="register-form"
          class="px-8 flex flex-col"
          action="/login"
          method="POST"
        >

          <label for="user_name" class="font-bold">First Name</label>
          <input
            type="text"
            id="user_name"
            name="user_name"
            placeholder="John"
            class="px-4 py-2 border border-gray-300 rounded-lg mt-2 focus:outline-none focus:ring-2 focus:ring-gray-200 focus:border-transparent"
            autocomplete="name"
            required
          />

          <label for="user_lastname" class="font-bold mt-4">Last Name</label>
          <input
            type="text"
            id="user_lastname"
            name="user_lastname"
            placeholder="Doe"
            class="px-4 py-2 border border-gray-300 rounded-lg mt-2 focus:outline-none focus:ring-2 focus:ring-gray-200 focus:border-transparent"
            autocomplete="family-name"
            required
          />

          <label for="user_email" class="font-bold mt-4">Email</label>
          <input
            type="email"
            id="user_email"
            name="user_email"
            placeholder="johndoe@example.com"
            class="px-4 py-2 border border-gray-300 rounded-lg mt-2 focus:outline-none focus:ring-2 focus:ring-gray-200 focus:border-transparent"
            autocomplete="email"
            required
          />

          <lable for="user_age" class="font-bold mt-4">Age</lable>
          <input
            type="number"
            id="user_age"
            name="user_age"
            placeholder="18"
            class="px-4 py-2 border border-gray-300 rounded-lg mt-2 focus:outline-none focus:ring-2 focus:ring-gray-200 focus:border-transparent"
            required
          />

          <div class="mt-4">
            <label for="user_password" class="font-bold">Password</label>
            <div class="flex">
              <input
                type="password"
                id="user_password"
                name="user_password"
                placeholder="********"
                autocomplete="current-password"
                class="px-4 py-2 border border-gray-300 rounded-lg mt-2 text-black w-full"
                required
              />
              <div
                style="margin-left: -35px; margin-top: 20px"
                id="showPasswordIcon"
              >
                <i
                  class="fa-solid fa-eye-slash text-lg hover:cursor-pointer"
                ></i>
              </div>

              <div
                class="hidden"
                style="margin-left: -35px; margin-top: 20px; display: none"
                id="hidePasswordIcon"
              >
                <i class="fa-solid fa-eye text-lg hover:cursor-pointer"></i>
              </div>
            </div>
          </div>

          <button
            type="submit"
            class="bg-black text-white px-4 py-2 rounded-lg mt-6 hover:bg-emerald-700 transition-all duration-300"
          >Create an account</button>
        </form>

        <a
          href="/"
          class="block text-center mt-6 mb-8 text-gray-600 hover:underline"
        >Already have an account? Log in now</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
  <script src="/js/register.js"></script>
</div>