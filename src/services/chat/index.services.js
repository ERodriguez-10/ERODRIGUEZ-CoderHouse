import { addMessage } from "#services/chat/addMessage.services.js";
import { getMessages } from "#services/chat/getMessages.services.js";

export const chatServices = {
  addMessage,
  getMessages,
};
